import { config,ethers } from "hardhat"

async function dump_test_accounts(){
const private_keys: string[] =[]
const accounts =config.networks.hardhat.accounts
for (let i=0;i<50;i++){

}
}

dump_test_accounts()